# dstack v0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the dstack CLI binary + Claude Code plugin delivering persistent memory, multi-repo sync, VPS deployment, and quality gates.

**Architecture:** Rust workspace with 2 crates (dstack-memory lib + dstack-cli bin). Plugin directory with 6 skills, 3 hooks, 3 commands. Config via TOML. FileProvider default, ErukaProvider optional.

**Tech Stack:** Rust (clap, serde, toml, reqwest, chrono, tokio), Bash (hooks), Markdown (skills/commands)

---

## File Structure

### Rust Crates
- `Cargo.toml` — workspace root
- `crates/dstack-memory/src/lib.rs` — MemoryProvider trait + Field struct
- `crates/dstack-memory/src/file.rs` — FileProvider (JSON files)
- `crates/dstack-memory/src/eruka.rs` — ErukaProvider (REST API)
- `crates/dstack-cli/src/main.rs` — CLI entrypoint (clap subcommands)
- `crates/dstack-cli/src/config.rs` — Config loading (TOML)
- `crates/dstack-cli/src/cmd_memory.rs` — `dstack memory` subcommand
- `crates/dstack-cli/src/cmd_deploy.rs` — `dstack deploy` subcommand
- `crates/dstack-cli/src/cmd_sync.rs` — `dstack sync` subcommand
- `crates/dstack-cli/src/cmd_audit.rs` — `dstack audit` subcommand

### Plugin
- `plugin/package.json` — Plugin metadata
- `plugin/CLAUDE.md` — Bootstrap instructions
- `plugin/hooks/hooks.json` — Hook definitions
- `plugin/hooks/session-start` — Bash: calls `dstack memory load`
- `plugin/hooks/quality-gate` — Bash: calls `dstack audit --pre-commit`
- `plugin/hooks/context-monitor` — Bash: calls `dstack context-monitor`
- `plugin/skills/using-dstack/SKILL.md`
- `plugin/skills/eruka-memory/SKILL.md`
- `plugin/skills/multi-repo-ops/SKILL.md`
- `plugin/skills/vps-deploy/SKILL.md`
- `plugin/skills/companion-docs/SKILL.md`
- `plugin/skills/quality-gates/SKILL.md`
- `plugin/commands/deploy.md`
- `plugin/commands/sync.md`
- `plugin/commands/audit.md`

### Config & Docs
- `overlays/example.toml` — Example private overlay
- `README.md`
- `LICENSE`

---

### Task 1: Workspace Scaffold + Config Crate

**Files:**
- Create: `Cargo.toml`
- Create: `crates/dstack-memory/Cargo.toml`
- Create: `crates/dstack-cli/Cargo.toml`
- Create: `crates/dstack-cli/src/config.rs`
- Create: `crates/dstack-cli/src/main.rs` (minimal)

- [ ] **Step 1: Create workspace Cargo.toml**

```toml
[workspace]
resolver = "2"
members = ["crates/dstack-memory", "crates/dstack-cli"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/dirmacs/dstack"
```

- [ ] **Step 2: Create dstack-memory Cargo.toml**

```toml
[package]
name = "dstack-memory"
version.workspace = true
edition.workspace = true

[dependencies]
async-trait = "0.1"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["json"], optional = true }
thiserror = "2"
tokio = { version = "1", features = ["fs"] }

[features]
default = ["file"]
file = []
eruka = ["reqwest"]
```

- [ ] **Step 3: Create dstack-cli Cargo.toml**

```toml
[package]
name = "dstack-cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "dstack"
path = "src/main.rs"

[dependencies]
dstack-memory = { path = "../dstack-memory", features = ["file", "eruka"] }
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
tokio = { version = "1", features = ["full"] }
dirs = "6"
anyhow = "1"
```

- [ ] **Step 4: Create config.rs with TOML parsing**

```rust
// crates/dstack-cli/src/config.rs
use serde::Deserialize;
use std::collections::HashMap;
use std::path::PathBuf;

#[derive(Debug, Deserialize)]
pub struct Config {
    #[serde(default)]
    pub memory: MemoryConfig,
    #[serde(default)]
    pub repos: RepoConfig,
    #[serde(default)]
    pub deploy: HashMap<String, DeployTarget>,
    #[serde(default)]
    pub git: GitConfig,
}

#[derive(Debug, Deserialize)]
pub struct MemoryConfig {
    #[serde(default = "default_backend")]
    pub backend: String,
    #[serde(default = "default_memory_path")]
    pub path: String,
    #[serde(default)]
    pub eruka: ErukaConfig,
}

#[derive(Debug, Default, Deserialize)]
pub struct ErukaConfig {
    #[serde(default)]
    pub url: String,
    #[serde(default)]
    pub service_key: String,
}

#[derive(Debug, Deserialize)]
pub struct RepoConfig {
    #[serde(default = "default_repo_root")]
    pub root: String,
    #[serde(default)]
    pub tracked: Vec<String>,
}

#[derive(Debug, Deserialize)]
pub struct DeployTarget {
    pub build: String,
    pub service: String,
    #[serde(default)]
    pub smoke: Option<String>,
}

#[derive(Debug, Default, Deserialize)]
pub struct GitConfig {
    #[serde(default)]
    pub author_name: Option<String>,
    #[serde(default)]
    pub author_email: Option<String>,
}

fn default_backend() -> String { "file".into() }
fn default_memory_path() -> String { "~/.dstack/memory".into() }
fn default_repo_root() -> String { "/opt".into() }

impl Default for MemoryConfig {
    fn default() -> Self {
        Self {
            backend: default_backend(),
            path: default_memory_path(),
            eruka: ErukaConfig::default(),
        }
    }
}

impl Default for RepoConfig {
    fn default() -> Self {
        Self { root: default_repo_root(), tracked: vec![] }
    }
}

impl Config {
    pub fn load() -> anyhow::Result<Self> {
        let path = config_path();
        if path.exists() {
            let content = std::fs::read_to_string(&path)?;
            Ok(toml::from_str(&content)?)
        } else {
            Ok(Config {
                memory: MemoryConfig::default(),
                repos: RepoConfig::default(),
                deploy: HashMap::new(),
                git: GitConfig::default(),
            })
        }
    }

    pub fn memory_path(&self) -> PathBuf {
        let p = self.memory.path.replace("~", &dirs::home_dir().unwrap().display().to_string());
        PathBuf::from(p)
    }

    pub fn eruka_service_key(&self) -> String {
        std::env::var("DSTACK_ERUKA_KEY")
            .unwrap_or_else(|_| self.memory.eruka.service_key.clone())
    }
}

pub fn config_path() -> PathBuf {
    dirs::config_dir()
        .unwrap_or_else(|| PathBuf::from("~/.config"))
        .join("dstack")
        .join("config.toml")
}
```

- [ ] **Step 5: Create minimal main.rs that loads config**

```rust
// crates/dstack-cli/src/main.rs
mod config;

use clap::{Parser, Subcommand};

#[derive(Parser)]
#[command(name = "dstack", version, about = "Development stack for AI-assisted multi-repo work")]
struct Cli {
    #[command(subcommand)]
    command: Commands,
}

#[derive(Subcommand)]
enum Commands {
    /// Show current configuration
    Config,
}

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    let cli = Cli::parse();
    let cfg = config::Config::load()?;

    match cli.command {
        Commands::Config => {
            println!("Config path: {}", config::config_path().display());
            println!("Memory backend: {}", cfg.memory.backend);
            println!("Memory path: {}", cfg.memory_path().display());
            println!("Tracked repos: {:?}", cfg.repos.tracked);
        }
    }

    Ok(())
}
```

- [ ] **Step 6: Build and verify**

Run: `cd /opt/dstack && cargo build`
Expected: Compiles with 0 errors.

- [ ] **Step 7: Test config loading**

Run: `cd /opt/dstack && cargo run -- config`
Expected: Prints default config values (no config.toml exists yet, so defaults).

- [ ] **Step 8: Commit**

```bash
cd /opt/dstack
git init
git add Cargo.toml crates/ docs/
git -c user.name="bkataru" -c user.email="baalateja.k@gmail.com" commit -m "feat: workspace scaffold with config system"
```

---

### Task 2: MemoryProvider Trait + Field Struct

**Files:**
- Create: `crates/dstack-memory/src/lib.rs`

- [ ] **Step 1: Write the test**

```rust
// crates/dstack-memory/src/lib.rs
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn field_serialization_roundtrip() {
        let field = Field {
            path: "projects/ehb/learnings/tag-pipeline".into(),
            value: "All agent prompts need ---TAGS--- instruction".into(),
            confidence: 0.95,
            source: "user_correction".into(),
            updated_at: chrono::Utc::now(),
        };
        let json = serde_json::to_string(&field).unwrap();
        let parsed: Field = serde_json::from_str(&json).unwrap();
        assert_eq!(parsed.path, field.path);
        assert_eq!(parsed.confidence, 0.95);
    }

    #[test]
    fn field_default_confidence() {
        let field = Field::new("test/path", "value", "test");
        assert_eq!(field.confidence, 0.5);
        assert_eq!(field.source, "test");
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd /opt/dstack && cargo test -p dstack-memory`
Expected: FAIL — Field struct not defined.

- [ ] **Step 3: Implement lib.rs**

```rust
// crates/dstack-memory/src/lib.rs
use async_trait::async_trait;
use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};

pub mod file;
#[cfg(feature = "eruka")]
pub mod eruka;

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Field {
    pub path: String,
    pub value: String,
    pub confidence: f64,
    pub source: String,
    pub updated_at: DateTime<Utc>,
}

impl Field {
    pub fn new(path: impl Into<String>, value: impl Into<String>, source: impl Into<String>) -> Self {
        Self {
            path: path.into(),
            value: value.into(),
            confidence: 0.5,
            source: source.into(),
            updated_at: Utc::now(),
        }
    }

    pub fn with_confidence(mut self, confidence: f64) -> Self {
        self.confidence = confidence.clamp(0.0, 1.0);
        self
    }
}

#[derive(Debug, thiserror::Error)]
pub enum MemoryError {
    #[error("IO error: {0}")]
    Io(#[from] std::io::Error),
    #[error("Serialization error: {0}")]
    Serde(#[from] serde_json::Error),
    #[error("HTTP error: {0}")]
    Http(String),
    #[error("Not found: {0}")]
    NotFound(String),
}

pub type Result<T> = std::result::Result<T, MemoryError>;

#[async_trait]
pub trait MemoryProvider: Send + Sync {
    /// Load all fields matching a path prefix
    async fn load(&self, path: &str) -> Result<Vec<Field>>;
    /// Write a single field (upsert by path)
    async fn write(&self, field: &Field) -> Result<()>;
    /// Search fields by keyword in value
    async fn search(&self, query: &str) -> Result<Vec<Field>>;
    /// Delete a field by exact path
    async fn delete(&self, path: &str) -> Result<()>;
    /// Export all fields
    async fn export_all(&self) -> Result<Vec<Field>>;
}
```

- [ ] **Step 4: Run tests**

Run: `cd /opt/dstack && cargo test -p dstack-memory`
Expected: 2 tests pass (need empty file.rs and eruka.rs stubs first).

- [ ] **Step 5: Create stubs for file.rs and eruka.rs**

```rust
// crates/dstack-memory/src/file.rs
// FileProvider — implemented in Task 3

// crates/dstack-memory/src/eruka.rs
// ErukaProvider — implemented in Task 4
```

- [ ] **Step 6: Run tests, verify pass**

Run: `cd /opt/dstack && cargo test -p dstack-memory`
Expected: PASS — 2 tests.

- [ ] **Step 7: Commit**

```bash
git add crates/dstack-memory/
git -c user.name="bkataru" -c user.email="baalateja.k@gmail.com" commit -m "feat: MemoryProvider trait + Field struct"
```

---

### Task 3: FileProvider Implementation

**Files:**
- Create: `crates/dstack-memory/src/file.rs`

- [ ] **Step 1: Write failing tests**

```rust
// crates/dstack-memory/src/file.rs — tests at bottom
#[cfg(test)]
mod tests {
    use super::*;
    use tempfile::TempDir;

    async fn test_provider() -> (FileProvider, TempDir) {
        let dir = TempDir::new().unwrap();
        let provider = FileProvider::new(dir.path().to_path_buf());
        (provider, dir)
    }

    #[tokio::test]
    async fn write_and_load() {
        let (provider, _dir) = test_provider().await;
        let field = Field::new("project/test/key1", "hello world", "test");
        provider.write(&field).await.unwrap();

        let loaded = provider.load("project/test").await.unwrap();
        assert_eq!(loaded.len(), 1);
        assert_eq!(loaded[0].value, "hello world");
    }

    #[tokio::test]
    async fn search_finds_match() {
        let (provider, _dir) = test_provider().await;
        provider.write(&Field::new("a/b", "rust compiler error", "test")).await.unwrap();
        provider.write(&Field::new("a/c", "python import issue", "test")).await.unwrap();

        let results = provider.search("rust").await.unwrap();
        assert_eq!(results.len(), 1);
        assert_eq!(results[0].path, "a/b");
    }

    #[tokio::test]
    async fn delete_removes_field() {
        let (provider, _dir) = test_provider().await;
        provider.write(&Field::new("x/y", "val", "test")).await.unwrap();
        provider.delete("x/y").await.unwrap();
        let loaded = provider.load("x").await.unwrap();
        assert!(loaded.is_empty());
    }

    #[tokio::test]
    async fn load_empty_returns_empty() {
        let (provider, _dir) = test_provider().await;
        let loaded = provider.load("nonexistent").await.unwrap();
        assert!(loaded.is_empty());
    }

    #[tokio::test]
    async fn export_all_returns_everything() {
        let (provider, _dir) = test_provider().await;
        provider.write(&Field::new("a/1", "v1", "test")).await.unwrap();
        provider.write(&Field::new("b/2", "v2", "test")).await.unwrap();
        let all = provider.export_all().await.unwrap();
        assert_eq!(all.len(), 2);
    }
}
```

- [ ] **Step 2: Run tests to verify failure**

Run: `cd /opt/dstack && cargo test -p dstack-memory -- file`
Expected: FAIL — FileProvider not defined.

- [ ] **Step 3: Implement FileProvider**

```rust
// crates/dstack-memory/src/file.rs
use crate::{Field, MemoryError, MemoryProvider, Result};
use async_trait::async_trait;
use std::path::PathBuf;
use tokio::fs;

/// File-based memory provider. Stores each field as a JSON file.
/// Path "projects/ehb/learnings/tags" → `{root}/projects/ehb/learnings/tags.json`
pub struct FileProvider {
    root: PathBuf,
}

impl FileProvider {
    pub fn new(root: PathBuf) -> Self {
        Self { root }
    }

    fn field_path(&self, field_path: &str) -> PathBuf {
        let sanitized = field_path.replace("..", "").replace('\0', "");
        self.root.join(format!("{}.json", sanitized))
    }

    async fn read_field(&self, path: &std::path::Path) -> Result<Field> {
        let content = fs::read_to_string(path).await?;
        Ok(serde_json::from_str(&content)?)
    }

    async fn all_json_files(&self) -> Result<Vec<PathBuf>> {
        let mut files = vec![];
        let mut stack = vec![self.root.clone()];
        while let Some(dir) = stack.pop() {
            if let Ok(mut entries) = fs::read_dir(&dir).await {
                while let Ok(Some(entry)) = entries.next_entry().await {
                    let path = entry.path();
                    if path.is_dir() {
                        stack.push(path);
                    } else if path.extension().map(|e| e == "json").unwrap_or(false) {
                        files.push(path);
                    }
                }
            }
        }
        Ok(files)
    }
}

#[async_trait]
impl MemoryProvider for FileProvider {
    async fn load(&self, path: &str) -> Result<Vec<Field>> {
        let mut results = vec![];
        for file in self.all_json_files().await? {
            if let Ok(field) = self.read_field(&file).await {
                if field.path.starts_with(path) {
                    results.push(field);
                }
            }
        }
        results.sort_by(|a, b| b.updated_at.cmp(&a.updated_at));
        Ok(results)
    }

    async fn write(&self, field: &Field) -> Result<()> {
        let path = self.field_path(&field.path);
        if let Some(parent) = path.parent() {
            fs::create_dir_all(parent).await?;
        }
        let json = serde_json::to_string_pretty(field)?;
        fs::write(path, json).await?;
        Ok(())
    }

    async fn search(&self, query: &str) -> Result<Vec<Field>> {
        let query_lower = query.to_lowercase();
        let mut results = vec![];
        for file in self.all_json_files().await? {
            if let Ok(field) = self.read_field(&file).await {
                if field.value.to_lowercase().contains(&query_lower)
                    || field.path.to_lowercase().contains(&query_lower)
                {
                    results.push(field);
                }
            }
        }
        Ok(results)
    }

    async fn delete(&self, path: &str) -> Result<()> {
        let file_path = self.field_path(path);
        if file_path.exists() {
            fs::remove_file(&file_path).await?;
        }
        Ok(())
    }

    async fn export_all(&self) -> Result<Vec<Field>> {
        let mut results = vec![];
        for file in self.all_json_files().await? {
            if let Ok(field) = self.read_field(&file).await {
                results.push(field);
            }
        }
        Ok(results)
    }
}
```

- [ ] **Step 4: Add tempfile dev-dependency**

Add to `crates/dstack-memory/Cargo.toml`:
```toml
[dev-dependencies]
tempfile = "3"
tokio = { version = "1", features = ["full"] }
```

- [ ] **Step 5: Run tests, verify pass**

Run: `cd /opt/dstack && cargo test -p dstack-memory -- file`
Expected: PASS — 5 tests.

- [ ] **Step 6: Commit**

```bash
git add crates/dstack-memory/
git -c user.name="bkataru" -c user.email="baalateja.k@gmail.com" commit -m "feat: FileProvider — JSON-based memory backend"
```

---

### Task 4: ErukaProvider Implementation

**Files:**
- Create: `crates/dstack-memory/src/eruka.rs`

- [ ] **Step 1: Write failing tests**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn eruka_provider_creation() {
        let provider = ErukaProvider::new("http://localhost:8081", "test-key");
        assert_eq!(provider.base_url, "http://localhost:8081");
    }

    #[tokio::test]
    async fn unreachable_eruka_returns_error() {
        let provider = ErukaProvider::new("http://localhost:99999", "fake-key");
        let result = provider.load("test").await;
        assert!(result.is_err());
    }

    #[test]
    fn empty_service_key_detected() {
        let provider = ErukaProvider::new("http://localhost:8081", "");
        assert!(provider.service_key.is_empty());
    }
}
```

- [ ] **Step 2: Implement ErukaProvider**

```rust
// crates/dstack-memory/src/eruka.rs
use crate::{Field, MemoryError, MemoryProvider, Result};
use async_trait::async_trait;
use serde::Deserialize;

pub struct ErukaProvider {
    pub base_url: String,
    pub service_key: String,
    client: reqwest::Client,
}

#[derive(Debug, Deserialize)]
struct ErukaContextResponse {
    #[serde(default)]
    fields: Vec<ErukaField>,
}

#[derive(Debug, Deserialize)]
struct ErukaField {
    field_path: String,
    value: String,
    #[serde(default)]
    confidence: Option<f64>,
    #[serde(default)]
    updated_at: Option<String>,
}

impl ErukaProvider {
    pub fn new(base_url: impl Into<String>, service_key: impl Into<String>) -> Self {
        Self {
            base_url: base_url.into(),
            service_key: service_key.into(),
            client: reqwest::Client::builder()
                .timeout(std::time::Duration::from_secs(5))
                .build()
                .unwrap_or_default(),
        }
    }

    fn map_field(ef: &ErukaField) -> Field {
        Field {
            path: ef.field_path.clone(),
            value: ef.value.clone(),
            confidence: ef.confidence.unwrap_or(0.5),
            source: "eruka".into(),
            updated_at: ef.updated_at.as_ref()
                .and_then(|s| chrono::DateTime::parse_from_rfc3339(s).ok())
                .map(|dt| dt.with_timezone(&chrono::Utc))
                .unwrap_or_else(chrono::Utc::now),
        }
    }
}

#[async_trait]
impl MemoryProvider for ErukaProvider {
    async fn load(&self, path: &str) -> Result<Vec<Field>> {
        let url = format!("{}/api/v1/context?path={}", self.base_url, path);
        let resp = self.client.get(&url)
            .header("X-Service-Key", &self.service_key)
            .send().await
            .map_err(|e| MemoryError::Http(e.to_string()))?;
        if !resp.status().is_success() {
            return Err(MemoryError::Http(format!("HTTP {}", resp.status())));
        }
        let body: ErukaContextResponse = resp.json().await
            .map_err(|e| MemoryError::Http(e.to_string()))?;
        Ok(body.fields.iter().map(Self::map_field).collect())
    }

    async fn write(&self, field: &Field) -> Result<()> {
        let url = format!("{}/api/v1/context", self.base_url);
        let resp = self.client.post(&url)
            .header("X-Service-Key", &self.service_key)
            .json(&serde_json::json!({
                "path": field.path,
                "value": field.value,
                "source": field.source,
                "confidence": field.confidence,
            }))
            .send().await
            .map_err(|e| MemoryError::Http(e.to_string()))?;
        if !resp.status().is_success() {
            let body = resp.text().await.unwrap_or_default();
            return Err(MemoryError::Http(format!("Write failed: {}", body)));
        }
        Ok(())
    }

    async fn search(&self, query: &str) -> Result<Vec<Field>> {
        let url = format!("{}/api/v1/context/search?q={}", self.base_url, query);
        let resp = self.client.get(&url)
            .header("X-Service-Key", &self.service_key)
            .send().await
            .map_err(|e| MemoryError::Http(e.to_string()))?;
        if !resp.status().is_success() {
            // Fallback: load all and filter client-side
            return self.load("").await.map(|fields| {
                fields.into_iter()
                    .filter(|f| f.value.to_lowercase().contains(&query.to_lowercase()))
                    .collect()
            });
        }
        let body: ErukaContextResponse = resp.json().await
            .map_err(|e| MemoryError::Http(e.to_string()))?;
        Ok(body.fields.iter().map(Self::map_field).collect())
    }

    async fn delete(&self, path: &str) -> Result<()> {
        let url = format!("{}/api/v1/context?path={}", self.base_url, path);
        let resp = self.client.delete(&url)
            .header("X-Service-Key", &self.service_key)
            .send().await
            .map_err(|e| MemoryError::Http(e.to_string()))?;
        if !resp.status().is_success() {
            return Err(MemoryError::Http(format!("Delete failed: HTTP {}", resp.status())));
        }
        Ok(())
    }

    async fn export_all(&self) -> Result<Vec<Field>> {
        self.load("").await
    }
}
```

- [ ] **Step 3: Run tests**

Run: `cd /opt/dstack && cargo test -p dstack-memory`
Expected: PASS — 8 tests (5 file + 3 eruka).

- [ ] **Step 4: Commit**

```bash
git add crates/dstack-memory/
git -c user.name="bkataru" -c user.email="baalateja.k@gmail.com" commit -m "feat: ErukaProvider — REST API memory backend"
```

---

### Task 5: CLI Memory Commands

**Files:**
- Create: `crates/dstack-cli/src/cmd_memory.rs`
- Modify: `crates/dstack-cli/src/main.rs`

- [ ] **Step 1: Implement cmd_memory.rs**

```rust
// crates/dstack-cli/src/cmd_memory.rs
use crate::config::Config;
use dstack_memory::{Field, MemoryProvider};

pub async fn provider_from_config(cfg: &Config) -> anyhow::Result<Box<dyn MemoryProvider>> {
    match cfg.memory.backend.as_str() {
        "eruka" => {
            let key = cfg.eruka_service_key();
            Ok(Box::new(dstack_memory::eruka::ErukaProvider::new(
                &cfg.memory.eruka.url, &key,
            )))
        }
        _ => {
            let path = cfg.memory_path();
            std::fs::create_dir_all(&path)?;
            Ok(Box::new(dstack_memory::file::FileProvider::new(path)))
        }
    }
}

pub async fn load(cfg: &Config, project: Option<&str>) -> anyhow::Result<()> {
    let provider = provider_from_config(cfg).await?;
    let path = project.unwrap_or("");
    let fields = provider.load(path).await?;
    if fields.is_empty() {
        eprintln!("No memory fields found for path: {}", if path.is_empty() { "(all)" } else { path });
        return Ok(());
    }
    for f in &fields {
        println!("[{:.0}%] {} = {}", f.confidence * 100.0, f.path, f.value);
    }
    eprintln!("\n{} fields loaded.", fields.len());
    Ok(())
}

pub async fn save(cfg: &Config, key: &str, value: &str) -> anyhow::Result<()> {
    let provider = provider_from_config(cfg).await?;
    let field = Field::new(key, value, "user").with_confidence(0.9);
    provider.write(&field).await?;
    eprintln!("Saved: {} = {}", key, value);
    Ok(())
}

pub async fn query(cfg: &Config, pattern: &str) -> anyhow::Result<()> {
    let provider = provider_from_config(cfg).await?;
    let results = provider.search(pattern).await?;
    if results.is_empty() {
        eprintln!("No matches for: {}", pattern);
        return Ok(());
    }
    for f in &results {
        println!("[{:.0}%] {} = {}", f.confidence * 100.0, f.path, f.value);
    }
    Ok(())
}

pub async fn export(cfg: &Config) -> anyhow::Result<()> {
    let provider = provider_from_config(cfg).await?;
    let all = provider.export_all().await?;
    println!("{}", serde_json::to_string_pretty(&all)?);
    Ok(())
}
```

- [ ] **Step 2: Wire into main.rs**

```rust
// crates/dstack-cli/src/main.rs
mod config;
mod cmd_memory;

use clap::{Parser, Subcommand};

#[derive(Parser)]
#[command(name = "dstack", version, about = "Development stack for AI-assisted multi-repo work")]
struct Cli {
    #[command(subcommand)]
    command: Commands,
}

#[derive(Subcommand)]
enum Commands {
    /// Show current configuration
    Config,
    /// Memory operations (load, save, query, export)
    Memory {
        #[command(subcommand)]
        action: MemoryAction,
    },
}

#[derive(Subcommand)]
enum MemoryAction {
    /// Load memory fields for a project
    Load {
        #[arg(short, long)]
        project: Option<String>,
    },
    /// Save a key-value pair to memory
    Save {
        key: String,
        value: String,
    },
    /// Search memory by keyword
    Query {
        pattern: String,
    },
    /// Export all memory as JSON
    Export,
}

#[tokio::main]
async fn main() -> anyhow::Result<()> {
    let cli = Cli::parse();
    let cfg = config::Config::load()?;

    match cli.command {
        Commands::Config => {
            println!("Config path: {}", config::config_path().display());
            println!("Memory backend: {}", cfg.memory.backend);
            println!("Memory path: {}", cfg.memory_path().display());
            println!("Tracked repos: {:?}", cfg.repos.tracked);
            println!("Deploy targets: {:?}", cfg.deploy.keys().collect::<Vec<_>>());
        }
        Commands::Memory { action } => match action {
            MemoryAction::Load { project } => {
                cmd_memory::load(&cfg, project.as_deref()).await?;
            }
            MemoryAction::Save { key, value } => {
                cmd_memory::save(&cfg, &key, &value).await?;
            }
            MemoryAction::Query { pattern } => {
                cmd_memory::query(&cfg, &pattern).await?;
            }
            MemoryAction::Export => {
                cmd_memory::export(&cfg).await?;
            }
        },
    }

    Ok(())
}
```

- [ ] **Step 3: Build and test**

Run: `cd /opt/dstack && cargo build && cargo run -- memory save test/greeting "hello dstack" && cargo run -- memory load --project test && cargo run -- memory query hello && cargo run -- memory export`
Expected: Each command works — saves field, loads it, searches it, exports JSON.

- [ ] **Step 4: Commit**

```bash
git add crates/dstack-cli/
git -c user.name="bkataru" -c user.email="baalateja.k@gmail.com" commit -m "feat: CLI memory commands (load, save, query, export)"
```

---

### Task 6: CLI Deploy + Sync + Audit Commands

**Files:**
- Create: `crates/dstack-cli/src/cmd_deploy.rs`
- Create: `crates/dstack-cli/src/cmd_sync.rs`
- Create: `crates/dstack-cli/src/cmd_audit.rs`
- Modify: `crates/dstack-cli/src/main.rs`

- [ ] **Step 1: Implement cmd_deploy.rs**

```rust
// crates/dstack-cli/src/cmd_deploy.rs
use crate::config::{Config, DeployTarget};
use std::process::Command;

pub fn deploy(cfg: &Config, service: &str) -> anyhow::Result<()> {
    let target = cfg.deploy.get(service)
        .ok_or_else(|| anyhow::anyhow!("No deploy target '{}'. Available: {:?}", service, cfg.deploy.keys().collect::<Vec<_>>()))?;

    eprintln!("=== Deploying {} ===", service);

    // Build
    eprintln!("[1/3] Building...");
    let build_status = Command::new("bash").arg("-c").arg(&target.build).status()?;
    if !build_status.success() {
        anyhow::bail!("Build failed for {}", service);
    }

    // Restart
    eprintln!("[2/3] Restarting {}...", target.service);
    let restart = Command::new("sudo")
        .args(["systemctl", "restart", &target.service])
        .status()?;
    if !restart.success() {
        anyhow::bail!("Failed to restart {}", target.service);
    }

    // Smoke test
    if let Some(ref smoke) = target.smoke {
        eprintln!("[3/3] Smoke test...");
        std::thread::sleep(std::time::Duration::from_secs(2));
        let smoke_status = Command::new("bash").arg("-c").arg(smoke).status()?;
        if !smoke_status.success() {
            eprintln!("WARNING: Smoke test failed! Consider rollback.");
        } else {
            eprintln!("Smoke test passed.");
        }
    } else {
        eprintln!("[3/3] No smoke test configured.");
    }

    eprintln!("=== {} deployed ===", service);
    Ok(())
}

pub fn deploy_all(cfg: &Config) -> anyhow::Result<()> {
    if cfg.deploy.is_empty() {
        anyhow::bail!("No deploy targets configured.");
    }
    for service in cfg.deploy.keys() {
        deploy(cfg, service)?;
    }
    Ok(())
}
```

- [ ] **Step 2: Implement cmd_sync.rs**

```rust
// crates/dstack-cli/src/cmd_sync.rs
use crate::config::Config;
use std::process::Command;

pub fn status(cfg: &Config) -> anyhow::Result<()> {
    for repo in &cfg.repos.tracked {
        let path = format!("{}/{}", cfg.repos.root, repo);
        if !std::path::Path::new(&path).exists() {
            println!("{:<20} NOT FOUND", repo);
            continue;
        }
        let dirty = Command::new("git").args(["-C", &path, "status", "--porcelain"])
            .output()?.stdout;
        let dirty_count = String::from_utf8_lossy(&dirty).lines().count();
        let branch = Command::new("git").args(["-C", &path, "branch", "--show-current"])
            .output()?.stdout;
        let branch = String::from_utf8_lossy(&branch).trim().to_string();
        let status = if dirty_count > 0 { format!("{} dirty", dirty_count) } else { "clean".into() };
        println!("{:<20} {:<10} {}", repo, branch, status);
    }
    Ok(())
}

pub fn sync(cfg: &Config, dry_run: bool) -> anyhow::Result<()> {
    for repo in &cfg.repos.tracked {
        let path = format!("{}/{}", cfg.repos.root, repo);
        if !std::path::Path::new(&path).exists() {
            eprintln!("{}: NOT FOUND, skipping", repo);
            continue;
        }
        eprint!("{}: ", repo);
        let dirty = Command::new("git").args(["-C", &path, "status", "--porcelain"])
            .output()?.stdout;
        if !dirty.is_empty() {
            eprintln!("dirty — skipping (commit first)");
            continue;
        }
        if dry_run {
            eprintln!("clean (dry-run, would pull+push)");
            continue;
        }
        let pull = Command::new("git").args(["-C", &path, "pull", "--ff-only"]).status()?;
        if pull.success() {
            let push = Command::new("git").args(["-C", &path, "push"]).status()?;
            eprintln!("{}", if push.success() { "synced" } else { "push failed" });
        } else {
            eprintln!("pull failed (diverged?)");
        }
    }
    Ok(())
}
```

- [ ] **Step 3: Implement cmd_audit.rs**

```rust
// crates/dstack-cli/src/cmd_audit.rs
use crate::config::Config;

pub fn pre_commit() -> anyhow::Result<()> {
    let questions = [
        "Did I write negative tests?",
        "Did I verify against the LIVE system?",
        "Did I update the companion doc with DETAILS?",
        "Would these tests FAIL without my code change?",
        "Am I moving to the next step because current is TRULY done?",
    ];
    println!("=== dstack quality gate ===");
    for (i, q) in questions.iter().enumerate() {
        println!("  {}. {}", i + 1, q);
    }
    println!("\nReview these questions before committing.");
    println!("Run with --strict to require explicit confirmation.");
    Ok(())
}

pub fn stale(cfg: &Config) -> anyhow::Result<()> {
    eprintln!("Scanning for stale files across tracked repos...");
    for repo in &cfg.repos.tracked {
        let path = format!("{}/{}", cfg.repos.root, repo);
        // Check for .implementation.md files with stale status
        let find = std::process::Command::new("find")
            .args([&path, "-name", "*.implementation.md", "-mtime", "+14"])
            .output()?;
        let files = String::from_utf8_lossy(&find.stdout);
        for f in files.lines() {
            if !f.is_empty() {
                println!("STALE (>14d): {}", f);
            }
        }
    }
    Ok(())
}
```

- [ ] **Step 4: Wire all three into main.rs** — add Deploy, Sync, Audit to Commands enum and match arms. Follow the same pattern as Memory.

- [ ] **Step 5: Build and verify**

Run: `cd /opt/dstack && cargo build && cargo run -- sync --status && cargo run -- audit --pre-commit`
Expected: Sync shows repo status, audit prints the 5 questions.

- [ ] **Step 6: Commit**

```bash
git add crates/dstack-cli/
git -c user.name="bkataru" -c user.email="baalateja.k@gmail.com" commit -m "feat: CLI deploy, sync, audit commands"
```

---

### Task 7: Plugin — Skills (6 SKILL.md files)

**Files:**
- Create: `plugin/skills/using-dstack/SKILL.md`
- Create: `plugin/skills/eruka-memory/SKILL.md`
- Create: `plugin/skills/multi-repo-ops/SKILL.md`
- Create: `plugin/skills/vps-deploy/SKILL.md`
- Create: `plugin/skills/companion-docs/SKILL.md`
- Create: `plugin/skills/quality-gates/SKILL.md`

- [ ] **Step 1: Write all 6 skills** — each 50-80 lines, referencing `dstack` CLI commands. Content is derived from the spec (Section "Skills") plus existing patterns in `~/.claude/CLAUDE.md` and `~/.claude/skills/`.

- [ ] **Step 2: Verify skill format** — each has a trigger description, checklist, and references to CLI commands.

- [ ] **Step 3: Commit**

```bash
git add plugin/skills/
git -c user.name="bkataru" -c user.email="baalateja.k@gmail.com" commit -m "feat: 6 plugin skills (memory, multi-repo, deploy, docs, quality)"
```

---

### Task 8: Plugin — Hooks + Commands + Bootstrap

**Files:**
- Create: `plugin/package.json`
- Create: `plugin/CLAUDE.md`
- Create: `plugin/hooks/hooks.json`
- Create: `plugin/hooks/session-start`
- Create: `plugin/hooks/quality-gate`
- Create: `plugin/hooks/context-monitor`
- Create: `plugin/commands/deploy.md`
- Create: `plugin/commands/sync.md`
- Create: `plugin/commands/audit.md`

- [ ] **Step 1: Create package.json, CLAUDE.md, hooks.json, 3 hook scripts, 3 command files** — hooks call `dstack` CLI, commands are thin wrappers.

- [ ] **Step 2: Make hook scripts executable** — `chmod +x plugin/hooks/session-start plugin/hooks/quality-gate plugin/hooks/context-monitor`

- [ ] **Step 3: Commit**

```bash
git add plugin/
git -c user.name="bkataru" -c user.email="baalateja.k@gmail.com" commit -m "feat: plugin hooks, commands, and bootstrap"
```

---

### Task 9: README + LICENSE + Example Config

**Files:**
- Create: `README.md`
- Create: `LICENSE`
- Create: `overlays/example.toml`
- Create: `.gitignore`

- [ ] **Step 1: Write README** — installation, usage, compatibility with superpowers/GSD, architecture overview.

- [ ] **Step 2: Add MIT LICENSE**

- [ ] **Step 3: Create example overlay config and .gitignore**

- [ ] **Step 4: Commit**

```bash
git add README.md LICENSE overlays/ .gitignore
git -c user.name="bkataru" -c user.email="baalateja.k@gmail.com" commit -m "docs: README, LICENSE, example config"
```

---

### Task 10: Integration Test + GitHub Setup

**Files:**
- Modify: various (verification only)

- [ ] **Step 1: Build release binary**

Run: `cd /opt/dstack && cargo build --release`
Expected: Binary at `target/release/dstack`.

- [ ] **Step 2: Install locally and test full flow**

```bash
cp target/release/dstack /usr/local/bin/
dstack config
dstack memory save test/integration "dstack works"
dstack memory load --project test
dstack memory query works
dstack sync --status
dstack audit --pre-commit
```

- [ ] **Step 3: Create GitHub repo and push**

```bash
gh repo create dirmacs/dstack --private --source=/opt/dstack --push
```

- [ ] **Step 4: Tag v0.1.0**

```bash
git -c user.name="bkataru" -c user.email="baalateja.k@gmail.com" tag -a v0.1.0 -m "dstack v0.1.0 — MVP"
git push origin v0.1.0
```

---

Plan complete and saved to `docs/plans/2026-04-08-dstack-v0.1-plan.md`. Two execution options:

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** — Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?